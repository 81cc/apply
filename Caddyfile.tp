http://:${PORT} {
    root * /root/html
    file_server

    # WebSocket 反向代理到 V2Ray
    @websockets {
        header Connection *Upgrade*
        header Upgrade websocket
    }
    reverse_proxy @websockets 127.0.0.1:8080
}
